import useScrollReveal from "@/hooks/useScrollReveal";

const ScrollReveal = () => {
  {
    /* Component wrapper to lazy load useScrollReveal() */
  }
  useScrollReveal();
  return null;
};

export default ScrollReveal;
